from django import forms
from django.apps import AppConfig
# from .models import ImageModel

class SnapappConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'snapapp'

